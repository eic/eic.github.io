---
# ----------------------------------------------------------------------------
# Copy this file to _people/<yourname>.md and edit. Everything except `layout`
# and `name` is optional - delete the lines you do not need.
# ----------------------------------------------------------------------------
layout: person

name: Your Name
shortname: yourname          # must match the filename, used for the edit link

role: What you do in ePIC    # one line, shown under your name
institution: Your Institute

photo:                       # /assets/images/people/yourname.jpg
email:
github:                      # just the username
orcid:                       # just the identifier, e.g. 0000-0002-1825-0097
website:

# Only if you convene or coordinate something. `office` is one of the entries in
# _data/org.yml (software, technical, analysis, spokesperson) and `wg` is one of
# the working groups listed under it. `kind` is convener, deputy or lead; drop
# `wg` for an office-level role. Delete the whole block otherwise - you will
# still appear in the "Everyone" section of the People page.
#roles:
#  - office: software
#    wg: user-learning
#    kind: convener

# Detector subsystem roles (DSL, DSTC, ...). Free text, shown on your page only
# - the chart on the People page is the working-group chart, not the subsystem
# one. Delete if you have none.
#subsystems:
#  - Luminosity DSTC (Pair Spectrometer)

active: true
verified: true               # you wrote this yourself, so it is verified
search_exclude: true         # remove this line - it only hides the template
---

Write whatever you like here: a paragraph about what you work on is plenty.
Markdown headings, lists and links all work.
